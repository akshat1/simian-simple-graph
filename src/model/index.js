import Edge from './Edge';
import Graph, { nodesFromEdges } from './Graph';

/**
 * At the moment, a Node is just a string.
 * @typedef {string} Node
 */

export {
  Edge,
  Graph,
  nodesFromEdges,
}
